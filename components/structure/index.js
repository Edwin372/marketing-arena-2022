import React, { Component } from 'react';

class Structure extends Component {
    state = {  }
    render() {
        return (
            <div id="structure" className="session">
                Structure
            </div>

        );
    }
}

export default Structure;
